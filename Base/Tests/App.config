<?xml version="1.0"?>
<configuration>

  <connectionStrings>
		<add name="allors" connectionString="server=(local);database=Base;Integrated Security=SSPI, Application Name=Base"/>
	</connectionStrings>

</configuration>
