(function (module) {
    "use strict";

    var organisationController = function () {
        var vm = this;

    };

    module.controller("organisationController", organisationController);

}(angular.module("app")));
